export class Schedule {
    constructor (
        public no: number,
        public title: string,
        public start_date: Date,
        public end_date: Date,
        public member_no: number) { }
}