import { HighlightTodayDirective } from './highlight-today.directive';

describe('HighlightTodayDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTodayDirective();
    expect(directive).toBeTruthy();
  });
});
